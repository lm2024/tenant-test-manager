<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- 注册自定义转换器 -->
    <conversionRule conversionWord="tenant" 
                    converterClass="com.tenant.config.log.TenantLogConverter" />
    
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %tenant %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 专门针对Hibernate SQL的日志配置 -->
    <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
        <appender-ref ref="STDOUT" />
    </logger>
    
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" additivity="false">
        <appender-ref ref="STDOUT" />
    </logger>
    
    <!-- 应用日志 -->
    <logger name="com.tenant" level="DEBUG" additivity="false">
        <appender-ref ref="STDOUT" />
    </logger>
    
    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
    
</configuration> 